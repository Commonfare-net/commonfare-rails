<%# Toastr configuration is in flash_setup.coffee %>
<script type="text/javascript">
<% flash.each do |name, message| %>
  <% name = "info" if name == "notice" %>
  <% name = "error" if name == "alert" %>
  toastr.<%= name %>(<%= message.inspect.html_safe %>);
<% end %>
</script>
