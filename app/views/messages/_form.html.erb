<%= simple_form_for([message.messageable.group, message.messageable, message]) do |f| %>
  <%= f.error_notification %>
  <%= f.association :commoner, as: :hidden %>
  <%= f.input :body, as: :text, label: false %>
  <%= f.button :submit, class: 'btn btn-cf-alt float-right' %>
<% end %>
