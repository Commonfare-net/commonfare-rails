<%= simple_form_for(message_form_args(message)) do |f| %>
  <%= f.error_notification %>
  <%= f.association :commoner, as: :hidden %>
  <%= f.input :body, as: :text, label: false %>
  <%= f.button :submit, s_('Button|Send'), class: 'btn btn-cf-alt float-right' %>
<% end %>
