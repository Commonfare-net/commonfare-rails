<%= simple_form_for(comment, url: commentable_comment_path_for(commentable)) do |f| %>
  <%= render 'comments/fields', f: f, btn_text: user_signed_in? ? s_('Button|Comment') : s_('Button|Login and comment') %>
<% end %>
