<div class="row justify-content-center">
  <div class="col-md-8 col-lg-6">
    <hr>
    <%= simple_form_for(comment, url: story_comments_path(story)) do |f| %>
      <%= render 'comments/fields', f: f, btn_text: s_('Publish comment button|Comment') %>
    <% end %>
  </div>
</div>
