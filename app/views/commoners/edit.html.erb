<div class="row justify-content-center">
  <div class="col-md-6 col-lg-4">
    <h2><%= _('Edit profile') %></h2>

    <%= render 'form', commoner: @commoner %>
    <p class="text-right">
      <%= link_to _('Edit your email or password'), edit_user_registration_path %>
    </p>
  </div>
</div>
