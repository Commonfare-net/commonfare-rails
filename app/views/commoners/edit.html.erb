<div class="row justify-content-center">
  <div class="col-md-6 col-lg-4">
    <h1><%= _('Edit profile') %></h1>

    <%= render 'form', commoner: @commoner %>
    <p class="text-right">
      <%= link_to _('Edit your credentials'), edit_user_registration_path %>
    </p>
  </div>
</div>
